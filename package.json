{
  "name": "patrol-board",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "description": "定期的にスクリプトを実行し、結果を GitHub Issue にダッシュボードとして表示・更新し続ける GitHub Action",
  "scripts": {
    "build": "ncc build src/main.ts --license licenses.txt",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
